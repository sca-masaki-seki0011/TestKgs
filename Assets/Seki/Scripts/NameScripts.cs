using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NameScripts : MonoBehaviour
{
    string myName = "";
    public string MYNAME {
        set {
            this.myName = value;
        }
        get {
            return this.myName;
        }
    }
        
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
